apiVersion: v1
kind: Pod
metadata:
  name: pod1
  namespace: web
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        cpu: "10m"
        memory: "50Mi"
      limits:
        cpu: "100m"
        memory: "128Mi"
