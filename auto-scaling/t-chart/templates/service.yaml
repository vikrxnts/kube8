apiVersion: v1
kind: Service
metadata:
  name: {{ include "t-chart.fullname" . }}
  labels:
    {{- include "t-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "t-chart.selectorLabels" . | nindent 4 }}
