apiVersion: v1
kind: Pod

metadata:
  name: pod1
  namespace: web

spec:
  containers:
    - name: web
      image: nginx
      imagePullPolicy: Never
      ports:
        - containerPort: 80
      env:
        - name: n1
          value: v1
