apiVersion: v1
kind: Pod

metadata:
  name: pod1
  namespace: web

spec:
  containers:
    - name: web
      image: nginx
      env:
        - name: test
          valueFrom:
            configMapKeyRef:
              key: name1
              name: envcm
